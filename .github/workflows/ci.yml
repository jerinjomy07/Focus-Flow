name: FocusFlow CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Quality Gates & Production Build
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
          POSTGRES_DB: focusflow
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    defaults:
      run:
        working-directory: focusflow-app

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: focusflow-app/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: TypeScript Type-Check
        run: npm run type-check

      - name: ESLint Static Analysis
        run: npm run lint

      - name: Push Database Schema
        run: npx prisma db push --skip-generate
        env:
          DATABASE_URL: "postgresql://postgres:password@localhost:5432/focusflow?schema=public"
          DIRECT_URL: "postgresql://postgres:password@localhost:5432/focusflow?schema=public"

      - name: Automated Test Suite (Unit & Component)
        run: npm test
        env:
          DATABASE_URL: "postgresql://postgres:password@localhost:5432/focusflow?schema=public"
          DIRECT_URL: "postgresql://postgres:password@localhost:5432/focusflow?schema=public"
          TEST_DATABASE_URL: "postgresql://postgres:password@localhost:5432/focusflow?schema=public"
          AUTH_SECRET: "ci_pipeline_dummy_auth_secret_minimum_32_characters_for_build"
          MOBILE_AUTH_SECRET: "ci_pipeline_dummy_mobile_auth_secret_minimum_32_characters"

      - name: Production Build (Turbopack)
        run: npm run build
        env:
          DATABASE_URL: "postgresql://postgres:password@localhost:5432/focusflow?schema=public"
          DIRECT_URL: "postgresql://postgres:password@localhost:5432/focusflow?schema=public"
          AUTH_SECRET: "ci_pipeline_dummy_auth_secret_minimum_32_characters_for_build"
          MOBILE_AUTH_SECRET: "ci_pipeline_dummy_mobile_auth_secret_minimum_32_characters"
          AUTH_URL: "http://localhost:3000"
          NEXT_PUBLIC_APP_URL: "http://localhost:3000"
          NEXT_PUBLIC_APP_VERSION: "1.0.0"

      - name: Dependency Security Audit
        run: npm audit || true
