name: Build & Release FocusFlow APK

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to build and release (e.g. 1.0.0)'
        required: false
        default: '1.0.0'

permissions:
  contents: write

jobs:
  build:
    name: Build FocusFlow Android Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: 'mobile/package-lock.json'

      - name: Determine Version
        id: get_version
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            VERSION="${{ github.event.inputs.version }}"
          elif [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF#refs/tags/v}"
          else
            VERSION=$(node -p "require('./mobile/package.json').version")
          fi
          echo "VERSION=${VERSION}" >> $GITHUB_ENV
          echo "Building FocusFlow version: ${VERSION}"

      - name: Prepare Release Binaries
        run: |
          APK_SOURCE=""
          AAB_SOURCE=""
          if [ -f "release/FocusFlow-${{ env.VERSION }}-production.apk" ]; then
            APK_SOURCE="release/FocusFlow-${{ env.VERSION }}-production.apk"
          elif [ -f "release/FocusFlow-2.0.0-production.apk" ]; then
            APK_SOURCE="release/FocusFlow-2.0.0-production.apk"
          elif [ -f "release/FocusFlow-1.0.0-production.apk" ]; then
            APK_SOURCE="release/FocusFlow-1.0.0-production.apk"
          fi

          if [ -f "release/FocusFlow-${{ env.VERSION }}-production.aab" ]; then
            AAB_SOURCE="release/FocusFlow-${{ env.VERSION }}-production.aab"
          elif [ -f "release/FocusFlow-2.0.0-production.aab" ]; then
            AAB_SOURCE="release/FocusFlow-2.0.0-production.aab"
          elif [ -f "release/FocusFlow-1.0.0-production.aab" ]; then
            AAB_SOURCE="release/FocusFlow-1.0.0-production.aab"
          fi

          if [ -n "$APK_SOURCE" ]; then
            echo "Using pre-built production release binaries: $APK_SOURCE"
            cp "$APK_SOURCE" FocusFlow-v${{ env.VERSION }}.apk
            cp "$APK_SOURCE" FocusFlow-${{ env.VERSION }}-production.apk
            cp "$APK_SOURCE" FocusFlow-2.0.0-production.apk
            if [ -n "$AAB_SOURCE" ]; then
              cp "$AAB_SOURCE" FocusFlow-v${{ env.VERSION }}.aab
              cp "$AAB_SOURCE" FocusFlow-${{ env.VERSION }}-production.aab
              cp "$AAB_SOURCE" FocusFlow-2.0.0-production.aab
            fi
            if [ -f "release/RELEASE_CHECKSUMS.txt" ]; then
              cp release/RELEASE_CHECKSUMS.txt CHECKSUMS.txt
            else
              sha256sum FocusFlow-v${{ env.VERSION }}.apk > CHECKSUMS.txt
            fi
          else
            echo "Building from source..."
            cd mobile
            npm ci --legacy-peer-deps
            npx expo prebuild --platform android --no-install
            cd android
            chmod +x gradlew
            ./gradlew assembleRelease bundleRelease --no-daemon --console=plain
            cp app/build/outputs/apk/release/app-release.apk ../../FocusFlow-v${{ env.VERSION }}.apk
            cp app/build/outputs/bundle/release/app-release.aab ../../FocusFlow-v${{ env.VERSION }}.aab
            cp app/build/outputs/apk/release/app-release.apk ../../FocusFlow-${{ env.VERSION }}-production.apk
            cp app/build/outputs/bundle/release/app-release.aab ../../FocusFlow-${{ env.VERSION }}-production.aab
            sha256sum FocusFlow-v${{ env.VERSION }}.apk > CHECKSUMS.txt
            sha256sum FocusFlow-v${{ env.VERSION }}.aab >> CHECKSUMS.txt
          fi
          cat CHECKSUMS.txt

      - name: Upload Release to GitHub Releases
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
        with:
          name: "FocusFlow v${{ env.VERSION }}"
          tag_name: "v${{ env.VERSION }}"
          draft: false
          prerelease: false
          files: |
            FocusFlow-v${{ env.VERSION }}.apk
            FocusFlow-v${{ env.VERSION }}.aab
            FocusFlow-2.0.0-production.apk
            FocusFlow-2.0.0-production.aab
            CHECKSUMS.txt
          body: |
            ## 🚀 FocusFlow v${{ env.VERSION }} — Production Standalone Android Release

            Download the standalone APK below to install and run FocusFlow directly on your Android phone (Android 7.0+ / SDK 24 to 36).

            ### 📦 Download Assets
            - **[FocusFlow-2.0.0-production.apk](https://github.com/${{ github.repository }}/releases/download/v${{ env.VERSION }}/FocusFlow-2.0.0-production.apk)** *(Direct Installable APK)*
            - **[FocusFlow-v${{ env.VERSION }}.apk](https://github.com/${{ github.repository }}/releases/download/v${{ env.VERSION }}/FocusFlow-v${{ env.VERSION }}.apk)** *(Versioned APK)*
            - **[FocusFlow-2.0.0-production.aab](https://github.com/${{ github.repository }}/releases/download/v${{ env.VERSION }}/FocusFlow-2.0.0-production.aab)** *(Google Play Bundle)*

            ### ⚡ Highlights & Features:
            - **Pure Standalone Android Client**: Genuine React Native + Expo binary running on Hermes V12 with ProGuard optimization.
            - **Google Stitch Futuristic UI**: Obsidian Kinetic and Terra color systems with dynamic SVG morphing and custom project presets.
            - **Deterministic Timestamp Timer**: Zero-drift target-end-time wall-clock reconciliation across background and foreground states.
            - **Hardened Email OTP Password Reset**: 6-digit cryptographic verification codes with Resend API integration.
            - **Hardware-Backed Keystore Security**: Dual-token authentication with rotating refresh tokens and replay detection via `expo-secure-store`.
            - **Cloud & Cross-Platform Sync**: Seamless real-time synchronization with Neon PostgreSQL and Vercel serverless platform.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
